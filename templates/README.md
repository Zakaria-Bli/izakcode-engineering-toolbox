# Templates

Project and feature starters meant to be copied into new work.

Templates are not active workspace packages by default. Add one to `pnpm-workspace.yaml` only if it must be validated as a runnable project.
